spring:
  application:
    name: treeflex-authorization-server
  profiles:
    active: dev
  cloud:
    config:
      uri: http://${CONFIGURATION_SERVER_HOSTNAME}:${CONFIGURATION_SERVER_PORT}
server:
  port: 8080
